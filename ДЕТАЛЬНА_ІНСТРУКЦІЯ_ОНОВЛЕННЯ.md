# 📖 ДЕТАЛЬНА ІНСТРУКЦІЯ: Як оновити файли на сервері

## 🎯 Мета: Оновити всі файли проекту на сервері через Git

---

## КРОК 1: Відкрити термінал (командний рядок)

### Варіант А: PowerShell (рекомендовано для Windows)

1. Натисніть клавіші `Windows + X`
2. Виберіть **"Windows PowerShell"** або **"Термінал"**
3. Або натисніть `Windows + R`, введіть `powershell` і натисніть Enter

### Варіант Б: CMD (Command Prompt)

1. Натисніть `Windows + R`
2. Введіть `cmd` і натисніть Enter

### Варіант В: Git Bash (якщо встановлений Git)

1. Клікніть правою кнопкою миші в будь-якій папці
2. Виберіть **"Git Bash Here"**

---

## КРОК 2: Підключитися до сервера через SSH

### 2.1. Введіть команду підключення:

```bash
ssh purelifebiz@91.223.223.246
```

**Пояснення:**
- `ssh` - команда для підключення через SSH
- `purelifebiz` - ваше ім'я користувача на сервері
- `@` - роздільник
- `91.223.223.246` - IP адреса сервера

### 2.2. Натисніть Enter

### 2.3. Перший раз може з'явитися запит:

```
The authenticity of host '91.223.223.246' can't be established.
ECDSA key fingerprint is SHA256:...
Are you sure you want to continue connecting (yes/no)?
```

**Що робити:**
- Введіть `yes` і натисніть Enter
- Це нормально при першому підключенні

### 2.4. Введіть пароль:

Коли побачите:
```
purelifebiz@91.223.223.246's password:
```

**Введіть пароль:** `PbPjLdzNSG=p`

**⚠️ ВАЖЛИВО:**
- Пароль не буде видно під час введення (це нормально!)
- Просто введіть пароль і натисніть Enter
- Якщо помилилися - натисніть Enter і спробуйте знову

### 2.5. Якщо підключення успішне:

Ви побачите щось на кшталт:
```
Welcome to Ubuntu...
purelifebiz@server:~$
```

Або просто:
```
[purelifebiz@server ~]$
```

**✅ Вітаю! Ви підключені до сервера!**

---

## КРОК 3: Знайти папку проекту на сервері

### 3.1. Перевірити поточну папку:

Введіть:
```bash
pwd
```

Натисніть Enter. Це покаже поточну папку.

### 3.2. Перейти в папку проекту:

Спробуйте один з цих варіантів (залежить від налаштування сервера):

**Варіант 1 (найчастіший):**
```bash
cd /home/purelifebiz/public_html
```

**Варіант 2:**
```bash
cd /home/purelifebiz/www
```

**Варіант 3:**
```bash
cd ~/public_html
```

**Варіант 4 (якщо є домен):**
```bash
cd /home/purelifebiz/domains
ls
# Подивіться список папок і перейдіть в потрібну
cd /home/purelifebiz/domains/ваш-домен/public_html
```

### 3.3. Перевірити, що ви в правильній папці:

Введіть:
```bash
ls -la
```

Натисніть Enter. Це покаже список файлів.

**Що шукати:**
- Якщо бачите файли типу `index.php`, `config.php`, `admin/` - ви в правильній папці ✅
- Якщо папка порожня або немає таких файлів - це теж нормально для першого разу

---

## КРОК 4: Перевірити, чи є Git репозиторій

### 4.1. Перевірити наявність Git:

Введіть:
```bash
git status
```

Натисніть Enter.

### 4.2. Два можливі результати:

#### ✅ Варіант А: Git репозиторій вже існує

Якщо бачите щось на кшталт:
```
On branch main
Your branch is behind 'origin/main' by X commits.
```

**Це означає:** Репозиторій вже є, потрібно тільки оновити!

**Перейдіть до КРОКУ 5А**

#### ❌ Варіант Б: Git репозиторій не існує

Якщо бачите помилку:
```
fatal: not a git repository
```

**Це означає:** Потрібно клонувати репозиторій вперше!

**Перейдіть до КРОКУ 5Б**

---

## КРОК 5А: Оновити існуючий репозиторій

### 5А.1. Отримати останні зміни з GitHub:

Введіть:
```bash
git pull origin main
```

Натисніть Enter.

### 5А.2. Результат:

Якщо все добре, побачите:
```
Updating abc1234..def5678
Fast-forward
 .../file1.php | 123 +++++++++++++++++++++++++++++++
 .../file2.php |  45 ++++++++++++
 8770 files changed, 1584622 insertions(+)
```

**✅ Готово! Файли оновлено!**

### 5А.3. Перевірити результат:

Введіть:
```bash
git status
```

Має показати:
```
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

**🎉 ВСЕ ГОТОВО! Файли оновлено на сервері!**

---

## КРОК 5Б: Клонувати репозиторій вперше

### 5Б.1. Переконайтеся, що ви в правильній папці:

```bash
pwd
```

### 5Б.2. Перевірте, чи папка порожня:

```bash
ls -la
```

**Якщо папка НЕ порожня:**
- Зробіть бекап існуючих файлів:
```bash
cd ..
mkdir backup_$(date +%Y%m%d)
cp -r public_html backup_$(date +%Y%m%d)/
cd public_html
```

### 5Б.3. Клонувати репозиторій:

**Варіант 1: Якщо папка порожня або ви хочете замінити все:**

```bash
git clone https://github.com/valet343/purelife.git .
```

**Пояснення:**
- `git clone` - команда для клонування
- `https://github.com/valet343/purelife.git` - адреса репозиторію
- `.` - означає "в поточну папку"

**Варіант 2: Якщо хочете в окрему папку:**

```bash
cd ..
git clone https://github.com/valet343/purelife.git purelife_new
cd purelife_new
```

### 5Б.4. Очікування:

Клонування може зайняти 1-5 хвилин, залежно від швидкості інтернету.

Ви побачите прогрес:
```
Cloning into '.'...
remote: Enumerating objects: 12345, done.
remote: Counting objects: 100% (12345/12345), done.
remote: Compressing objects: 100% (9876/9876), done.
Receiving objects: 100% (12345/12345), 150.50 MiB | 2.50 MiB/s, done.
Resolving deltas: 100% (5432/5432), done.
```

### 5Б.5. Перевірити результат:

```bash
ls -la
```

Маєте побачити всі файли проекту:
- `admin/`
- `catalog/`
- `system/`
- `config.php` (якщо він був на сервері раніше)
- та інші файли

**✅ Готово! Репозиторій клоновано!**

---

## КРОК 6: Налаштувати config файли (якщо потрібно)

### 6.1. Перевірити config файли:

```bash
ls -la config.php
ls -la admin/config.php
```

### 6.2. Якщо файлів немає:

Це нормально! Вони в `.gitignore` і не комітяться.

**Що робити:**
1. Створіть їх вручну на основі `config.php.dist` (якщо є)
2. Або скопіюйте з бекапу (якщо робили)
3. Або налаштуйте через адмін-панель OpenCart

### 6.3. Якщо файли є:

Перевірте, чи вони правильно налаштовані для продакшн сервера.

---

## КРОК 7: Перевірити права доступу

### 7.1. Перевірити права:

```bash
ls -la
```

### 7.2. Якщо потрібно змінити права:

```bash
# Для папок
find . -type d -exec chmod 755 {} \;

# Для файлів
find . -type f -exec chmod 644 {} \;

# Для папки з завантаженнями (якщо потрібно)
chmod -R 755 system/storage/
chmod -R 755 image/
```

---

## КРОК 8: Вихід з сервера

### 8.1. Вийти з SSH:

Введіть:
```bash
exit
```

Або натисніть `Ctrl + D`

### 8.2. Перевірити:

Ви повинні повернутися до локального терміналу:
```
C:\Users\YourName>
```

---

## ✅ ПІДСУМОК: Повна послідовність команд

### Якщо репозиторій вже існує:

```bash
ssh purelifebiz@91.223.223.246
# Введіть пароль: PbPjLdzNSG=p

cd /home/purelifebiz/public_html
git pull origin main
exit
```

### Якщо репозиторій не існує (перший раз):

```bash
ssh purelifebiz@91.223.223.246
# Введіть пароль: PbPjLdzNSG=p

cd /home/purelifebiz
git clone https://github.com/valet343/purelife.git public_html
cd public_html
exit
```

---

## 🆘 РОЗВ'ЯЗАННЯ ПРОБЛЕМ

### Проблема 1: "Permission denied"

**Помилка:**
```
Permission denied (publickey,password).
```

**Рішення:**
- Перевірте правильність пароля
- Переконайтеся, що вводите пароль правильно (він не видно)
- Спробуйте ввести пароль ще раз

### Проблема 2: "No such file or directory"

**Помилка:**
```
cd: /home/purelifebiz/public_html: No such file or directory
```

**Рішення:**
```bash
# Знайдіть правильний шлях
cd /home/purelifebiz
ls -la

# Або створіть папку
mkdir -p public_html
cd public_html
```

### Проблема 3: "fatal: not a git repository"

**Помилка:**
```
fatal: not a git repository (or any of the parent directories)
```

**Рішення:**
- Виконайте КРОК 5Б (клонування репозиторію)

### Проблема 4: "Permission denied" при git pull

**Помилка:**
```
error: cannot open .git/FETCH_HEAD: Permission denied
```

**Рішення:**
```bash
# Змінити власника файлів
sudo chown -R purelifebiz:purelifebiz /home/purelifebiz/public_html

# Або змінити права
chmod -R 755 /home/purelifebiz/public_html
```

### Проблема 5: "Host key verification failed"

**Помилка:**
```
Host key verification failed.
```

**Рішення:**
```bash
ssh-keyscan 91.223.223.246 >> ~/.ssh/known_hosts
```

---

## 📝 ДОДАТКОВІ КОМАНДИ ДЛЯ ПЕРЕВІРКИ

### Перевірити версію Git на сервері:
```bash
git --version
```

### Перевірити статус репозиторію:
```bash
git status
```

### Перевірити останні коміти:
```bash
git log --oneline -5
```

### Перевірити налаштування Git:
```bash
git config --list
```

### Перевірити remote репозиторії:
```bash
git remote -v
```

Має показати:
```
origin  https://github.com/valet343/purelife.git (fetch)
origin  https://github.com/valet343/purelife.git (push)
```

---

## 🎯 ШВИДКИЙ СТАРТ (для досвідчених)

Якщо ви вже знаєте шлях до проекту:

```bash
ssh purelifebiz@91.223.223.246
cd /home/purelifebiz/public_html && git pull origin main && exit
```

Введіть пароль коли запитає.

---

## 📞 ДОПОМОГА

Якщо щось не працює:
1. Перевірте правильність введених команд
2. Переконайтеся, що ви в правильній папці (`pwd`)
3. Перевірте права доступу (`ls -la`)
4. Перевірте підключення до інтернету на сервері

---

**Успіхів! 🚀**
