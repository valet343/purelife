# 📤 Як завантажити файл на сервер

## ✅ Що вже зроблено:

1. ✅ Файл `tetetest` додано до Git
2. ✅ Файл закомічено
3. ✅ Файл відправлено на GitHub

## 🚀 Тепер потрібно оновити на сервері:

### Крок 1: Підключіться до сервера

```bash
ssh purelifebiz@91.223.223.246
```

Введіть пароль: `PbPjLdzNSG=p`

### Крок 2: Перейдіть в папку проекту

```bash
cd /home/purelifebiz/public_html
```

### Крок 3: Оновіть файли з GitHub

```bash
git pull origin main
```

### Крок 4: Перевірте, що файл з'явився

```bash
ls -la tetetest
```

Або:

```bash
ls -la | grep tetetest
```

### Крок 5: Вихід

```bash
exit
```

---

## 📝 Повна послідовність команд (скопіюйте всі разом):

```bash
ssh purelifebiz@91.223.223.246
cd /home/purelifebiz/public_html
git pull origin main
ls -la tetetest
exit
```

Введіть пароль коли запитає.

---

## 🔄 Для майбутніх файлів - повний процес:

### Локально (на вашому комп'ютері):

```bash
# 1. Створити або змінити файл
# (наприклад, створити новий файл)

# 2. Додати файл до Git
git add назва_файлу

# 3. Закомітити
git commit -m "Опис змін"

# 4. Відправити на GitHub
git push origin main
```

### На сервері:

```bash
# 1. Підключитися
ssh purelifebiz@91.223.223.246

# 2. Перейти в папку
cd /home/purelifebiz/public_html

# 3. Оновити
git pull origin main

# 4. Вийти
exit
```

---

## ⚡ Швидкий спосіб (використовуючи скрипт):

Після того як ви створили/змінили файл локально:

```powershell
.\deploy-auto.ps1 "Додав файл tetetest"
```

Скрипт автоматично:
- Додасть файл до Git
- Закомітить
- Відправить на GitHub
- Запитає чи оновити на сервері

---

## 🎯 Поточний статус:

✅ Файл `tetetest` вже на GitHub  
⏳ Потрібно виконати `git pull` на сервері

Виконайте команди вище на сервері, і файл з'явиться!
