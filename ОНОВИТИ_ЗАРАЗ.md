# ⚡ ШВИДКЕ ОНОВЛЕННЯ НА СЕРВЕРІ ЗАРАЗ

## 📋 Ситуація:

✅ Файл `tetetest` вже на GitHub  
❌ Автоматичний деплой не спрацював (немає `plink`)  
✅ Потрібно оновити вручну на сервері

---

## 🚀 ЩО РОБИТИ ЗАРАЗ:

### Варіант 1: Через PowerShell/CMD (найпростіший)

Відкрийте PowerShell або CMD і виконайте:

```bash
ssh purelifebiz@91.223.223.246
```

**Пароль:** `PbPjLdzNSG=p`

Після підключення виконайте:

```bash
cd /home/purelifebiz/public_html
git pull origin main
ls -la tetetest
exit
```

---

### Варіант 2: Одна команда (якщо знаєте шлях)

```bash
ssh purelifebiz@91.223.223.246 "cd /home/purelifebiz/public_html && git pull origin main"
```

Введіть пароль коли запитає.

---

## 📝 Покроково:

1. **Відкрийте PowerShell** (Windows + X → PowerShell)

2. **Введіть команду:**
   ```bash
   ssh purelifebiz@91.223.223.246
   ```

3. **Введіть пароль:**
   ```
   PbPjLdzNSG=p
   ```
   (пароль не буде видно - це нормально)

4. **Перейдіть в папку:**
   ```bash
   cd /home/purelifebiz/public_html
   ```

5. **Оновіть файли:**
   ```bash
   git pull origin main
   ```

6. **Перевірте файл:**
   ```bash
   ls -la tetetest
   ```
   
   Має показати щось на кшталт:
   ```
   -rw-r--r-- 1 purelifebiz purelifebiz 0 ... tetetest
   ```

7. **Вихід:**
   ```bash
   exit
   ```

---

## ✅ Після виконання:

Файл `tetetest` з'явиться на сервері в папці `/home/purelifebiz/public_html/`

---

## 🔧 Для майбутнього (щоб автоматично працювало):

### Варіант А: Встановити PuTTY

1. Завантажте: https://www.putty.org/
2. Встановіть (включно з `plink.exe`)
3. Після цього скрипт `deploy-auto.ps1` працюватиме автоматично

### Варіант Б: Налаштувати SSH ключі (безпечніше)

Тоді не потрібен буде пароль при кожному підключенні.

---

**Виконайте команди вище зараз, і файл з'явиться на сервері!** 🚀
