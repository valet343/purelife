# Інструкція для оновлення файлів на сервері

## ✅ Що вже зроблено:

1. ✅ Всі файли закомічено локально (8770 файлів)
2. ✅ Файли відправлено на GitHub
3. ✅ Конфігурація SSH створена (`deploy-config.json`)

## 📋 Дані для підключення:

- **Хост:** 91.223.223.246
- **Користувач:** purelifebiz
- **Пароль:** PbPjLdzNSG=p
- **Шлях на сервері:** /home/purelifebiz/public_html (може відрізнятися)

## 🚀 Варіант 1: Оновлення вручну (найпростіший)

### Крок 1: Підключіться до сервера

Відкрийте термінал (PowerShell, CMD, або Git Bash) і виконайте:

```bash
ssh purelifebiz@91.223.223.246
```

Введіть пароль, коли запитає: `PbPjLdzNSG=p`

### Крок 2: Перейдіть в папку проекту

```bash
cd /home/purelifebiz/public_html
```

**Примітка:** Якщо цей шлях не існує, знайдіть правильний шлях:
```bash
pwd  # показати поточну папку
ls -la  # показати файли
```

Можливі варіанти шляху:
- `/home/purelifebiz/public_html`
- `/home/purelifebiz/www`
- `/home/purelifebiz/domains/ваш-домен/public_html`
- `/var/www/html`

### Крок 3: Оновіть файли

**Якщо Git репозиторій вже існує:**
```bash
git pull origin main
```

**Якщо Git репозиторій не існує (перший раз):**
```bash
# Спочатку переконайтеся, що папка порожня або створіть бекап
git clone https://github.com/valet343/purelife.git .
```

## 🚀 Варіант 2: Автоматичне оновлення (після встановлення PuTTY)

### Встановлення PuTTY:

1. Завантажте PuTTY: https://www.putty.org/
2. Встановіть його (включно з plink.exe)
3. Запустіть скрипт:

```powershell
.\update-server.ps1
```

## 🔧 Варіант 3: Використання Git Bash (якщо встановлено)

Якщо у вас встановлений Git for Windows, ви можете використати Git Bash:

```bash
# В Git Bash
ssh purelifebiz@91.223.223.246
# Введіть пароль: PbPjLdzNSG=p

cd /home/purelifebiz/public_html
git pull origin main
```

## 📝 Перевірка після оновлення

Після виконання `git pull`, перевірте:

```bash
# Перевірити статус
git status

# Перевірити останній коміт
git log -1

# Перевірити файли
ls -la
```

## ⚠️ Важливо

1. **Бекап:** Перед першим оновленням зробіть бекап існуючих файлів на сервері
2. **Права доступу:** Переконайтеся, що у користувача є права на запис у папку проекту
3. **Config файли:** Файли `config.php` та `admin/config.php` не комітяться (вони в `.gitignore`), тому їх потрібно налаштувати окремо на сервері

## 🆘 Усунення проблем

### Помилка "Permission denied"

```bash
# Перевірте права доступу
ls -la

# Якщо потрібно, змініть власника (замініть user:group на ваші)
sudo chown -R purelifebiz:purelifebiz /home/purelifebiz/public_html
```

### Помилка "Not a git repository"

Це означає, що Git репозиторій не ініціалізовано. Виконайте:

```bash
cd /home/purelifebiz/public_html
git clone https://github.com/valet343/purelife.git .
```

### Помилка "Host key verification failed"

```bash
ssh-keyscan 91.223.223.246 >> ~/.ssh/known_hosts
```

## 📞 Додаткова інформація

- GitHub репозиторій: https://github.com/valet343/purelife.git
- Гілка: main
- Конфігурація збережена в: `deploy-config.json` (не комітиться)

---

**Після першого налаштування** ви зможете використовувати скрипт `deploy-auto.ps1` для автоматичного деплою!
