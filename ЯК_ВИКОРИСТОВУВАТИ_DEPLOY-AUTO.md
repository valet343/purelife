# 📖 Як використовувати скрипт deploy-auto.ps1

## 🎯 Призначення:

Скрипт автоматично:
1. ✅ Додає зміни до Git
2. ✅ Створює коміт
3. ✅ Відправляє на GitHub
4. ✅ Оновлює файли на сервері через SSH

---

## 🚀 Як запустити:

### Варіант 1: З описом змін (рекомендовано)

Відкрийте PowerShell в папці проекту і виконайте:

```powershell
.\deploy-auto.ps1 "Опис ваших змін"
```

**Приклади:**

```powershell
.\deploy-auto.ps1 "Додав новий файл tetetest"
```

```powershell
.\deploy-auto.ps1 "Оновив файли в папці admin"
```

```powershell
.\deploy-auto.ps1 "Виправив помилку в config.php"
```

```powershell
.\deploy-auto.ps1 "Додав нову функцію оплати"
```

### Варіант 2: Без опису (використає стандартний)

```powershell
.\deploy-auto.ps1
```

Скрипт використає стандартне повідомлення: "Оновлення проекту"

---

## 📝 Покрокова інструкція:

### Крок 1: Відкрити PowerShell

**Спосіб А:** Відкрити PowerShell в папці проекту
1. Відкрийте папку `c:\OSPanel\domains\p` в Провіднику
2. Натисніть `Shift + Права кнопка миші` в папці
3. Виберіть **"Відкрити окно PowerShell здесь"** або **"Open PowerShell window here"**

**Спосіб Б:** Відкрити PowerShell і перейти в папку
1. Натисніть `Windows + X`
2. Виберіть **"Windows PowerShell"**
3. Введіть:
   ```powershell
   cd c:\OSPanel\domains\p
   ```

### Крок 2: Запустити скрипт

Введіть команду з описом ваших змін:

```powershell
.\deploy-auto.ps1 "Ваш опис змін"
```

**Приклад:**
```powershell
.\deploy-auto.ps1 "Додав новий файл для тестування"
```

### Крок 3: Відповідати на запити скрипта

Скрипт буде задавати питання:

#### 3.1. Якщо є незакомічені зміни:

```
Знайдено незакомічені зміни:
  Не в staging area:
    tetetest
    новий_файл.php

Додати всі зміни до коміту? (y/n):
```

**Введіть:** `y` і натисніть Enter

#### 3.2. Запит на повідомлення коміту:

```
Введіть повідомлення коміту (Enter для використання: 'Додав новий файл для тестування'):
```

**Варіанти:**
- Натисніть Enter - використає запропоноване повідомлення
- Або введіть своє повідомлення і натисніть Enter

#### 3.3. Підтвердження відправки на GitHub:

```
Відправити зміни на GitHub? (y/n):
```

**Введіть:** `y` і натисніть Enter

#### 3.4. Підтвердження деплою на сервер:

```
Виконати деплой на сервер? (y/n):
```

**Введіть:** `y` і натисніть Enter

### Крок 4: Очікування

Скрипт виконає всі операції автоматично. Ви побачите прогрес:

```
=== Перевірка статусу Git ===
=== Додавання змін до staging area...
✓ Зміни додані
=== Створення коміту...
✓ Коміт успішно створено!
=== Відправка на GitHub ===
✓ Відправлено на GitHub
=== Деплой на сервер через SSH ===
✓ Деплой на сервер виконано успішно!
```

---

## 💡 Приклади використання:

### Приклад 1: Додавання нового файлу

```powershell
# Ви створили новий файл test.php
.\deploy-auto.ps1 "Додав файл test.php"
```

### Приклад 2: Зміна існуючого файлу

```powershell
# Ви змінили файл config.php
.\deploy-auto.ps1 "Оновив налаштування в config.php"
```

### Приклад 3: Кілька змін

```powershell
# Ви змінили кілька файлів
.\deploy-auto.ps1 "Оновив дизайн та виправив помилки"
```

### Приклад 4: Без опису

```powershell
# Скрипт використає стандартне повідомлення
.\deploy-auto.ps1
```

---

## ⚙️ Параметри скрипта:

### Параметр 1: Повідомлення коміту

```powershell
.\deploy-auto.ps1 "Ваш опис"
```

### Параметр 2: Пропустити коміт (якщо вже закомічено)

```powershell
.\deploy-auto.ps1 -SkipCommit
```

Це корисно, якщо ви вже створили коміт вручну і потрібно тільки відправити на сервер.

---

## 🔧 Вимоги:

### Перед використанням переконайтеся:

1. ✅ **PuTTY встановлено** (для автоматичного SSH деплою)
   - Завантажити: https://www.putty.org/
   - Встановити з `plink.exe`

2. ✅ **Конфігурація SSH створена**
   - Файл `deploy-config.json` має існувати
   - Якщо немає, запустіть: `.\setup-deploy-config.ps1`

3. ✅ **Git налаштовано**
   - Репозиторій ініціалізовано
   - Remote `origin` налаштовано

---

## ❌ Можливі проблеми:

### Проблема 1: "plink не знайдено"

**Помилка:**
```
⚠ plink не знайдено. Встановіть PuTTY або виконайте вручну
```

**Рішення:**
1. Встановіть PuTTY: https://www.putty.org/
2. Переконайтеся, що `plink.exe` в PATH
3. Або виконайте оновлення на сервері вручну

### Проблема 2: "deploy-config.json не знайдено"

**Помилка:**
```
Помилка: deploy-config.json не знайдено!
```

**Рішення:**
```powershell
.\setup-deploy-config.ps1
```

### Проблема 3: "Execution Policy"

**Помилка:**
```
cannot be loaded because running scripts is disabled on this system
```

**Рішення:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема 4: "Permission denied" на сервері

**Рішення:**
Перевірте права доступу на сервері:
```bash
ssh purelifebiz@91.223.223.246
cd /home/purelifebiz/public_html
sudo chown -R purelifebiz:purelifebiz .
```

---

## 📋 Повна послідовність (швидкий старт):

```powershell
# 1. Відкрити PowerShell в папці проекту
cd c:\OSPanel\domains\p

# 2. Запустити скрипт
.\deploy-auto.ps1 "Опис ваших змін"

# 3. Відповідати на запити:
# - "Додати всі зміни до коміту? (y/n):" → y
# - "Відправити зміни на GitHub? (y/n):" → y
# - "Виконати деплой на сервер? (y/n):" → y

# 4. Готово! ✅
```

---

## 🎯 Типові сценарії:

### Сценарій 1: Щоденна робота

```powershell
# Зробили зміни в коді
.\deploy-auto.ps1 "Виправив помилку в модулі оплати"
```

### Сценарій 2: Додавання нового функціоналу

```powershell
# Додали нову функцію
.\deploy-auto.ps1 "Додав новий модуль доставки"
```

### Сценарій 3: Оновлення вже закомічених змін

```powershell
# Вже закомітили вручну, потрібно тільки відправити
.\deploy-auto.ps1 -SkipCommit
```

---

## ✅ Перевірка після виконання:

Після успішного виконання:

1. **Перевірте на GitHub:**
   - Відкрийте: https://github.com/valet343/purelife
   - Переконайтеся, що коміт з'явився

2. **Перевірте на сервері:**
   ```bash
   ssh purelifebiz@91.223.223.246
   cd /home/purelifebiz/public_html
   git log -1
   ls -la ваш_файл
   ```

---

## 💻 Альтернатива (якщо скрипт не працює):

Якщо скрипт не працює, виконайте вручну:

```powershell
# Локально
git add .
git commit -m "Опис змін"
git push origin main

# На сервері (через SSH)
ssh purelifebiz@91.223.223.246
cd /home/purelifebiz/public_html
git pull origin main
exit
```

---

**Готово! Тепер ви знаєте, як використовувати скрипт! 🚀**
