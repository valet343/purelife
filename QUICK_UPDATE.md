# 🚀 Швидке оновлення файлів на сервері

## ✅ Що вже готово:

- ✅ Всі 8770 файлів закомічено
- ✅ Файли відправлено на GitHub
- ✅ Конфігурація SSH збережена

## 📋 Швидка інструкція:

### Варіант 1: Через PowerShell/CMD (найпростіший)

Відкрийте PowerShell або CMD і виконайте:

```bash
ssh purelifebiz@91.223.223.246
```

**Пароль:** `PbPjLdzNSG=p`

Після підключення:

```bash
cd /home/purelifebiz/public_html
git pull origin main
```

Якщо репозиторію немає (перший раз):
```bash
cd /home/purelifebiz/public_html
git clone https://github.com/valet343/purelife.git .
```

### Варіант 2: Через Git Bash

Якщо у вас встановлений Git for Windows, відкрийте **Git Bash** і виконайте ті самі команди.

## 📝 Повна послідовність команд:

```bash
# 1. Підключення
ssh purelifebiz@91.223.223.246

# 2. Введіть пароль коли запитає
# PbPjLdzNSG=p

# 3. Перехід в папку проекту
cd /home/purelifebiz/public_html

# 4. Оновлення (якщо репозиторій вже є)
git pull origin main

# АБО клонування (якщо перший раз)
# git clone https://github.com/valet343/purelife.git .
```

## ⚠️ Важливо:

1. **Шлях може відрізнятися!** Якщо `/home/purelifebiz/public_html` не існує, знайдіть правильний шлях:
   ```bash
   pwd  # показати поточну папку
   ls -la  # показати файли
   ```

2. **Бекап:** Перед першим оновленням зробіть бекап існуючих файлів

3. **Config файли:** `config.php` та `admin/config.php` не комітяться - налаштуйте їх окремо на сервері

## 🔄 Для майбутніх оновлень:

Після першого налаштування використовуйте:

```powershell
.\deploy-auto.ps1 "Опис змін"
```

Або вручну:
```bash
# Локально
git add .
git commit -m "Опис змін"
git push origin main

# На сервері
ssh purelifebiz@91.223.223.246
cd /home/purelifebiz/public_html
git pull origin main
```

---

**Готово!** Виконайте команди вище для оновлення файлів на сервері.
