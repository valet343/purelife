# Швидкий старт з Git

## ✅ Що вже зроблено:
- Git встановлено та налаштовано
- Ім'я: `seotm-zt`
- Email: `72516055+seotm-zt@users.noreply.github.com`
- Репозиторій ініціалізовано
- Всі файли додані до staging area

## 🚀 Що робити далі:

### 1. Додати Git до PATH (щоб працювала команда `git`)

**Варіант А:** Запустити скрипт (для поточної сесії):
```powershell
.\git-setup.ps1
```

**Варіант Б:** Додати постійно:
1. `Win + R` → `sysdm.cpl` → Enter
2. Вкладка "Додатково" → "Змінні середовища"
3. У "Системні змінні" знайдіть `Path` → "Змінити"
4. "Створити" → додайте: `C:\Program Files\Git\bin`
5. ОК → перезапустіть PowerShell

### 2. Додати віддалений репозиторій (remote)

Якщо у вас є репозиторій на GitHub/GitLab:
```bash
git remote add origin https://github.com/ВАШ_КОРИСТУВАЧ/ВАШ_РЕПОЗИТОРІЙ.git
```

### 3. Зробити commit

```bash
git commit -m "Перший commit: початкова версія проекту"
```

### 4. Завантажити на сервер (push)

```bash
git push -u origin main
```

## 📝 Типовий робочий процес:

```bash
# 1. Перевірити зміни
git status

# 2. Додати змінені файли
git add .

# 3. Зробити commit
git commit -m "Опис ваших змін"

# 4. Завантажити на сервер
git push
```

## ⚠️ Важливо:

- Перед кожним `push` перевіряйте зміни: `git status`
- Пишіть зрозумілі повідомлення в commit
- Якщо на сервері є нові зміни, спочатку виконайте: `git pull`

## 📚 Детальна інструкція:

Дивіться файл `GIT_INSTRUKCIYA.md` для повної інформації.





